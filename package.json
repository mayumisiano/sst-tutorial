{
  "name": "sst-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "sst dev",
    "deploy": "sst deploy",
    "remove": "sst remove",
    "console": "sst console",
    "test": "npm test --workspaces",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "sst": "^3",
    "typescript": "^5.1.6"
  },
  "workspaces": [
    "packages/*"
  ],
  "overrides": {
    "hono": "^4.12.0",
    "@modelcontextprotocol/sdk": "^1.26.0"
  }
}
